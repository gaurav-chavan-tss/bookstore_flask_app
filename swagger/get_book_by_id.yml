parameters:
  - in: path
    name: bookid
    description: ID of the book to retrieve
    required: true
    type: integer

responses:
  200:
    description: Book retrieved successfully
    schema:
      $ref: '#/definitions/Book'
